mixin box-actions-orders(link)
  div(
    class="input-group" 
    box-actions=link
  )
    select(class="custom-select")
      option(value='') Chọn hành động...
      option(value='confirm') Xác nhận
      option(value='initial') Hủy xác nhận
      option(value='delete') Xóa
    .input-group-append
      button(
        class="btn btn-outline-secondary"
        type='button'
      ) Áp dụng